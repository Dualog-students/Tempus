import { LimitDecimalPlacesDirective } from './limit-decimal-places.directive';

describe('LimitDecimalPlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitDecimalPlacesDirective();
    expect(directive).toBeTruthy();
  });
});
